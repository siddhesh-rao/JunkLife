{
  "name": "junklife-platform",
  "private": true,
  "version": "1.0.0",
  "description": "JunkLife is a production-ready scrap management platform for junklife.in.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
